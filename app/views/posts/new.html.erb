
<%= form_with(model: @post, local: true) do |f| %>

<%= render "shared/error_message", object: f.object %>

  <%= f.text_area :content, placeholder: "今日の積み上げ" %>

  <%= f.hidden_field :start_time, :value => Date.today %>

  <%= f.submit "POST" %>

<% end %>