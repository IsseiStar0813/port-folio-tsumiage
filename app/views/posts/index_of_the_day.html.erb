<div class = "flex justify-center mt-12">
  <div>
    <p class = "text-2xl mb-12"><%= @posted_day %>　の投稿</p>

    <% @posts.each do |post|%> 
      <p class = "my-1 text-xl hover:underline hover:text-purple-700"> <%= link_to(post.title, post_path(post.id)) %></p> 
    <% end %>
  </div>
</div>

